#!/usr/bin/env rust

fn test_byte_max_value() {
    let b: byte = 255;
    println!("{}", b);
    assert_eq!(b, 255);
}

fn test_byte_zero() {
    let zero: byte = 0;
    println!("{}", zero);
    assert_eq!(zero, 0);
}

fn main() {
    test_byte_max_value();
    test_byte_zero();
}
