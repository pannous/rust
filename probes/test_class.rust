#!/usr/bin/env rust
# class is an alias for struct, commas between fields are optional
class Point {
	x: i32
	y: i32
}

point := Point{x: 10, y: 20}
put!("Point: (%d, %d)\n", point.x, point.y)
eq!(point.x, 10)
eq!(point.y, 20)
