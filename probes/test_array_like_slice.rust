#!/usr/bin/env rust
// import os

// Test 1-indexed array access
z := @['a', 'b', 'c']
if z#1 ≠ 'a' { put!("First element via #1 fails"); exit(0) }
// eq!( z#2 , 'b' ); // Second element
// eq!( z#3 , 'c' );

// Test with numbers
nums := @[10, 20, 30, 40]
// eq!( nums#1 , 10);
// eq!( nums#4 , 40);

// Test with expressions
idx := 2
if z#idx ≠ 'b' { put!("First element via #idx fails"); exit(0) }
// if not (z#idx == 'b') { put!("First element via #idx fails"); exit(0) }
put!("All tests passed successfully!")