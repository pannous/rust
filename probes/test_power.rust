#!/usr/bin/env rust
// Test power operator **

fn test_power_operator_parsing() {
    let a = 2;
    let b = 3;
    let result = a ** b;
    eq!(result, 8);
}

fn main() {
    test_power_operator_parsing();
}
